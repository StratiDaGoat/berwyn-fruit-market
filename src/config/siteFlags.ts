/** Promo banner feature flag — keep in sync with `class="has-promo-banner"` on `<html>` in index.html */
export const IS_CHIPS_AHOY_ACTIVE = true;

export const IS_FLASH_SALE_ACTIVE = false;
export const IS_BANANA_FLASH_ACTIVE = false;
